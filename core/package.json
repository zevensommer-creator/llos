{
  "name": "@llos/core",
  "version": "0.1.0",
  "private": true,
  "description": "LLOS privileged runtime: append-only learning events, deterministic projections, identity, entitlements",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node --test \"test/*.test.js\""
  },
  "dependencies": {
    "@llos/contracts": "workspace:*",
    "ts-fsrs": "^5.4.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0"
  }
}
