# CONVENTIONS.md — 代码与文档规范

> 本文件定义 LLOS 项目的代码风格、命名、文档格式统一约定。
> 所有 Agent 必须遵守，防止不同 Agent 的产出风格割裂。

---

## 1. 通用原则

- **中文注释，英文标识符**。代码注释、文档、提交信息用中文；变量/函数/类名用英文。
- **简洁优先**。不写冗余代码，不引入未使用的依赖。
- **每个模块单一职责**。文件不宜过大（建议单文件 < 400 行），过大应拆分。
- **明确接口边界**。模块之间通过明确定义的接口通信，不直接操作内部实现。

---

## 2. 目录结构

```
src/
├── core/          # 核心运行时：DLC 解析、训练执行、账户
├── market/        # DLC 市场：分类、发现、订阅
├── frontend/      # 前端：学生端、教师端、市场界面
├── agents/        # Agent 层：训练/评价/语音编排
└── api/           # API 网关与接口定义
```

新文件必须放入对应目录，不得随意散落。

---

## 3. 命名规范

| 类型 | 规范 | 示例 |
|------|------|------|
| 目录 | 小写 + 下划线 | `core/` `dlc_reference/` |
| 文件 | 小写 + 下划线 | `dlc_parser.py` |
| 类 | 大驼峰 PascalCase | `DlcParser` |
| 函数/方法 | 小驼峰 camelCase | `parseDlc()` |
| 常量 | 全大写 + 下划线 | `MAX_DLC_SIZE` |
| 变量 | 小驼峰 camelCase | `currentTaskId` |
| 数据库表 | 复数小写 + 下划线 | `users` `dlc_items` |

---

## 4. 代码风格

- 缩进统一用 **4 空格**（Python）/ **2 空格**（JS/TS）。
- 行尾不加空格，文件末尾保留一个空行。
- 字符串统一用双引号（除非语言惯例不同）。
- 函数必须有注释说明用途、参数、返回值。
- 错误处理：显式捕获并给出可读的中文错误信息，不静默吞掉异常。

---

## 5. 命名与术语（LLOS 特有）

| 术语 | 含义 | 禁止写法 |
|------|------|---------|
| DLC | Downloadable Learning Content，可下载学习内容 | 不要写成 "course/pack" 混淆 |
| Core | 通用能力层，不含具体语言/理论 | 不要在里面塞教学逻辑 |
| Market | DLC 市场 | 不要写成 "store" 混淆 |
| Account | 账户体系 | — |

**核心红线**：Core 中禁止出现任何具体语言（如德语）或具体教学理论（如 FSI/配价语法）的硬编码。这些一律属于 DLC。

---

## 6. 文档格式

- 文档统一使用 Markdown（`.md`）。
- 章节用 `#`，子节用 `##` / `###`，不跳级。
- 文档开头必须有"目的 + 范围"说明。
- 接口文档必须包含：请求/响应格式、错误码、示例。
- 中文文档，代码块/标识符用英文。

---

## 7. 提交信息规范

见 `AGENTS.md` 第 7 节：

```
<类型>: <一句话描述> (T-<任务号>)
```

类型：`feat` / `fix` / `docs` / `refactor` / `test` / `chore`

---

## 8. 测试规范

- 每个核心模块必须有对应测试，放在 `tests/`，命名 `test_<模块>.py`。
- 测试应独立可运行，不依赖外部服务（mock 掉 LLM/语音 API）。
- 提交前必须确认测试通过。

---

## 9. 规范更新

- 本规范只能由 Human 修改。
- 发现问题在 `TASKS.md` 提出，由 Human 决策。

---

*规范版本：1.0 · 最后更新：2026-08-09*