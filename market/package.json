{
  "name": "@llos/market",
  "version": "0.1.0",
  "private": true,
  "description": "LLOS DLC market backend: listings, catalog search/filter, entitlement-gated reviews, free acquisition via Core entitlements",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node --test \"test/*.test.js\""
  },
  "dependencies": {
    "@llos/core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0"
  }
}
