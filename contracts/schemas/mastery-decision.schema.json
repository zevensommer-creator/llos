{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:language-learning-platform:schema:mastery-decision:0.2.0",
  "title": "Mastery Decision",
  "description": "A revocable, recomputable decision for one claim under one evidence policy version, computed deterministically by the Core policy interpreter from a LearnerStateProjection. Not a permanent label on the learner. Policy upgrades produce new decisions alongside old ones and never rewrite history. The system must not store {learned: true, mastery_score: 0.86}-style permanent booleans or aggregate scores as source of truth (spec §5.1, §5.4).",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "decision_id",
    "learner_ref",
    "claim_ref",
    "evidence_policy_ref",
    "evidence_policy_version",
    "status",
    "reason_codes",
    "evidence_refs",
    "evaluated_at",
    "reducer_version"
  ],
  "properties": {
    "schema_version": {
      "const": "0.2.0"
    },
    "decision_id": {
      "$ref": "#/$defs/Identifier"
    },
    "learner_ref": {
      "$ref": "#/$defs/Identifier"
    },
    "claim_ref": {
      "$ref": "#/$defs/ClaimRef"
    },
    "evidence_policy_ref": {
      "$ref": "#/$defs/PolicyRef"
    },
    "evidence_policy_version": {
      "$ref": "#/$defs/SemVer"
    },
    "status": {
      "enum": [
        "not_yet",
        "provisional",
        "learned",
        "uncertain",
        "lapsed"
      ],
      "description": "Core-fixed decision states (spec §5.4). 'learned' means all necessary conditions of this evidence policy version are currently satisfied; it is a revocable decision bound to claim + policy version, not a permanent learner attribute. 'provisional' means immediate criteria are met but policy-declared retention/retest conditions are still pending."
    },
    "reason_codes": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "string",
        "pattern": "^[a-z][a-z0-9_.]*$"
      },
      "description": "Machine-readable explanation of which policy gates passed or failed. DLC may map codes to UI labels but cannot change their event semantics."
    },
    "evidence_refs": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/Identifier"
      },
      "description": "Identifiers of the evidence events or evidence groups backing this decision. May be empty for not_yet with no valid evidence yet."
    },
    "projection_ref": {
      "$ref": "#/$defs/Identifier",
      "description": "The LearnerStateProjection this decision was computed from; audit linkage only."
    },
    "evaluated_at": {
      "type": "string",
      "format": "date-time"
    },
    "valid_until": {
      "type": "string",
      "format": "date-time",
      "description": "Optional expiry declared by the policy; after it the decision must be re-evaluated (stale handling)."
    },
    "reducer_version": {
      "$ref": "#/$defs/SemVer"
    },
    "estimator_ref": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "id",
        "version"
      ],
      "properties": {
        "id": {
          "$ref": "#/$defs/Identifier"
        },
        "version": {
          "$ref": "#/$defs/SemVer"
        },
        "sha256": {
          "$ref": "#/$defs/Sha256"
        }
      },
      "description": "Optional versioned estimator/projector plugin (e.g. fsrs_memory). Estimates may inform review scheduling but never alone declare learned; raw LearningEvents remain the source of truth (spec §5.7)."
    },
    "supersedes_decision_id": {
      "$ref": "#/$defs/Identifier",
      "description": "Set when a policy-version upgrade produces a new decision for the same claim; the superseded decision is retained, never rewritten."
    },
    "extensions": {
      "$ref": "#/$defs/Extensions"
    }
  },
  "$defs": {
    "Identifier": {
      "type": "string",
      "minLength": 1,
      "maxLength": 128,
      "pattern": "^[A-Za-z0-9][A-Za-z0-9._:-]*$"
    },
    "ClaimRef": {
      "type": "string",
      "pattern": "^[A-Za-z0-9][A-Za-z0-9._-]*:claim/[A-Za-z0-9][A-Za-z0-9._-]*$"
    },
    "PolicyRef": {
      "type": "string",
      "pattern": "^[A-Za-z0-9][A-Za-z0-9._-]*:policy/[A-Za-z0-9][A-Za-z0-9._-]*$"
    },
    "SemVer": {
      "type": "string",
      "pattern": "^[0-9]+\\.[0-9]+\\.[0-9]+(?:-[0-9A-Za-z.-]+)?(?:\\+[0-9A-Za-z.-]+)?$"
    },
    "Sha256": {
      "type": "string",
      "pattern": "^[a-f0-9]{64}$"
    },
    "ExtensionEnvelope": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "schema_id",
        "schema_version",
        "payload_ref"
      ],
      "properties": {
        "schema_id": {
          "$ref": "#/$defs/Identifier"
        },
        "schema_version": {
          "$ref": "#/$defs/SemVer"
        },
        "payload_ref": {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "uri",
            "sha256"
          ],
          "properties": {
            "uri": {
              "type": "string",
              "format": "uri"
            },
            "sha256": {
              "$ref": "#/$defs/Sha256"
            }
          }
        }
      }
    },
    "Extensions": {
      "type": "object",
      "default": {},
      "propertyNames": {
        "pattern": "^[a-z][a-z0-9]*(?:[.-][a-z0-9]+)+$"
      },
      "additionalProperties": {
        "$ref": "#/$defs/ExtensionEnvelope"
      }
    }
  }
}
